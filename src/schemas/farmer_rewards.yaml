farmer_rewards:
  type: object
  properties:
    farmed_amount:
      type: integer
      example: 0
      format: uint64
    pool_reward_amount:
      type: integer
      example: 0
      format: uint64
    farmer_reward_amount:
      type: integer
      example: 0
      format: uint64
    fee_amount:
      type: integer
      example: 0
      format: uint64
    last_height_farmed:
      type: integer
      example: 0
      format: uint32
    blocks_won:
      type: integer
      example: 0
      format: uint32
    last_time_farmed:
      type: integer
      example: 0
      format: uint64
