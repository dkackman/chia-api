plugin_status:
  type: object
  properties:
    uploaders:
      type: object
      # a dictionary of dictionaries
      # uploaders: Dict[str, Dict[str, Any]] in the python
      additionalProperties:
        type: object
        additionalProperties: true
    downloaders:
      type: object
      additionalProperties:
        type: object
        additionalProperties: true
