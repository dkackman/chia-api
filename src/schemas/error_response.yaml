error_response:
  type: object
  description: Response in error case for all endpoints of the pool protocol
  properties:
    error_code:
      type: integer
      format: uint16
    error_message:
      type: string
