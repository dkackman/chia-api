vc_record:
  type: object
  properties:
    vc:
      $ref: ./verified_credential.yaml#/verified_credential
    confirmed_at_height:
      type: integer
      format: uint32
    coin_id:
      type: string
      format: hex
