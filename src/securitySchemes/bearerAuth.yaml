bearerAuth:
  type: http # this needs to be 'mutualTLS' which is supported in OAS 3.1.0 but tooling is lagging
  scheme: mutual
  description: Chia uses mutualTLS bearerAuth where client and server share TLS keys.
